/**
 * Fresher Minimal Template Mock Data
 *
 * Fresh Graduate persona - showcasing internships,
 * projects, and early career achievements.
 */

import { FRESH_GRADUATE_DATA } from '../../data/professionMockData';
import type { V2ResumeData } from '../../types/resumeData';

export const mockData: V2ResumeData = FRESH_GRADUATE_DATA;

export default mockData;
