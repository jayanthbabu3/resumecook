/**
 * Mono Elegant Template Config
 *
 * Re-exports the config from the centralized config location.
 */

export { monoElegantConfig as config } from '../../config/templates/mono-elegant';
export { default } from '../../config/templates/mono-elegant';
