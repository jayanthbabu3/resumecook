/**
 * Retail Manager Pro Template Configuration
 *
 * Re-exports the existing config from the config directory.
 */

export { retailManagerProConfig as config } from '../../config/templates/retail-manager-pro';
export { retailManagerProConfig } from '../../config/templates/retail-manager-pro';
