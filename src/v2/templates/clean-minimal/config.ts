/**
 * Clean Minimal Template Config
 *
 * Re-exports the config from the centralized config location.
 */

export { cleanMinimalConfig as config } from '../../config/templates/clean-minimal';
export { default } from '../../config/templates/clean-minimal';
