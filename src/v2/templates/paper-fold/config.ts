/**
 * Paper Fold Template Config
 *
 * Re-exports the config from the centralized config location.
 */

export { paperFoldConfig as config } from '../../config/templates/paper-fold';
export { default } from '../../config/templates/paper-fold';
