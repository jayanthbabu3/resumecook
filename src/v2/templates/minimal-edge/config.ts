/**
 * Minimal Edge Template Config
 *
 * Re-exports the config from the centralized config location.
 */

export { minimalEdgeConfig as config } from '../../config/templates/minimal-edge';
export { default } from '../../config/templates/minimal-edge';
