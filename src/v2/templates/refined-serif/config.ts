/**
 * Refined Serif Template Config
 *
 * Re-exports the config from the centralized config location.
 */

export { refinedSerifConfig as config } from '../../config/templates/refined-serif';
export { default } from '../../config/templates/refined-serif';
