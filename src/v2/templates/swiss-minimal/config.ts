/**
 * Swiss Minimal Template Config
 *
 * Re-exports the config from the centralized config location.
 */

export { swissMinimalConfig as config } from '../../config/templates/swiss-minimal';
export { default } from '../../config/templates/swiss-minimal';
