/**
 * Accent Stripe Template Config
 *
 * Re-exports the config from the centralized config location.
 */

export { accentStripeConfig as config } from '../../config/templates/accent-stripe';
export { default } from '../../config/templates/accent-stripe';
