/**
 * Modern Elegance Template Config
 *
 * Re-exports the config from the centralized config location.
 */

export { modernEleganceConfig as config } from '../../config/templates/modern-elegance';
export { default } from '../../config/templates/modern-elegance';
