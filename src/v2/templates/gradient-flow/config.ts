/**
 * Gradient Flow Template Config
 *
 * Re-exports the config from the centralized config location.
 */

export { gradientFlowConfig as config } from '../../config/templates/gradient-flow';
export { default } from '../../config/templates/gradient-flow';
