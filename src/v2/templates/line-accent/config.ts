/**
 * Line Accent Template Config
 *
 * Re-exports the config from the centralized config location.
 */

export { lineAccentConfig as config } from '../../config/templates/line-accent';
export { default } from '../../config/templates/line-accent';
