/**
 * Theater Actor Pro Template Configuration
 *
 * Re-exports the existing config from the config directory.
 */

export { theaterActorProConfig as config } from '../../config/templates/theater-actor-pro';
export { theaterActorProConfig } from '../../config/templates/theater-actor-pro';
