/**
 * DevOps Command Template Configuration
 *
 * Re-exports the config from the config directory.
 */

export { devopsCommandConfig as config } from '../../config/templates/devops-command';
export { devopsCommandConfig } from '../../config/templates/devops-command';
