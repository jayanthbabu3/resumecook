/**
 * CIO Executive Template Configuration
 *
 * Re-exports the existing config from the config directory.
 */

export { cioExecutiveConfig as config } from '../../config/templates/cio-executive';
export { cioExecutiveConfig } from '../../config/templates/cio-executive';
