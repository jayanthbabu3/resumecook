/**
 * Impact Leader Template Configuration
 *
 * Re-exports the existing config from the config directory.
 */

export { impactLeaderConfig as config } from '../../config/templates/impact-leader';
export { impactLeaderConfig } from '../../config/templates/impact-leader';
