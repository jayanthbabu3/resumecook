/**
 * Firebase Configuration - DEPRECATED STUB
 *
 * Firebase has been removed. This file exists for backwards compatibility.
 * All authentication and data storage now uses the API backend.
 */

// Stub exports for backwards compatibility
// These are null since Firebase is no longer used
export const firebaseApp = null as any;
export const app = null as any;
export const auth = null as any;
export const db = null as any;
export const storage = null as any;

// Stub type exports
export type User = any;
export type DocumentData = any;
export type DocumentSnapshot = any;
export type QuerySnapshot = any;

export default firebaseApp;
